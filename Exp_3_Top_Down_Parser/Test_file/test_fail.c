{
    i = 2;
    if (p == 2) {
        if(cite >= perm)
        {
            cite = perm;
        } else 
            break;
        } else {
        if (p <= 0) 
            cite = aol;
            perm = aol;
        /* The Following else should fail */
        else {
            perm = aol;
            p = aol;
        }
    }
}